apiVersion: v1
kind: Service
metadata:
  name: tandoor
spec:
  ports:
    # The image runs nginx in front of gunicorn and exposes port 80, unlike
    # most apps here which listen on a high port.
    - port: 80
  selector:
    app: tandoor
  type: ClusterIP
