# One id, shared by every workload that has cause to interrupt someone: the
# bridge (alerts, Flux, checks) and the morning briefing.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - campfire-mention-secret.yaml
