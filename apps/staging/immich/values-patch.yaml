apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  chart:
    spec:
      version: "0.10.3"
  values:
    controllers:
      main:
        containers:
          main:
            image:
              tag: v2.3.1
    "machine-learning":
      persistence:
        cache:
          storageClass: local-path
          accessMode: ReadWriteOnce
