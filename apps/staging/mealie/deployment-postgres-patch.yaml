apiVersion: apps/v1
kind: Deployment
metadata:
  name: mealie
spec:
  template:
    spec:
      containers:
        - name: mealie
          # Staging runs on a 2-core node. Base requests 100m for production;
          # observed staging usage is 5m. Memory requests are left alone —
          # CPU is the constraint here, memory sits at ~49%.
          resources:
            requests:
              cpu: 25m
          image: ghcr.io/mealie-recipes/mealie:v3.22.0
          env:
            - name: DB_ENGINE
              value: "postgres"
            - name: POSTGRES_SERVER
              value: "postgres-cluster-rw.database.svc.cluster.local"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DB
              value: "mealie"
            # Was the shared `app` role. Taken from the Secret so the role name
            # and its password can never drift apart (#219).
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-app-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-app-credentials
                  key: password
            - name: BASE_URL
              value: "https://mealie.staging.ronaldlokers.nl"
            - name: TZ
              value: "Europe/Amsterdam"
            - name: ALLOW_SIGNUP
              value: "false"
            - name: PUID
              value: "911"
            - name: PGID
              value: "911"
          volumeMounts:
            - name: mealie-data
              mountPath: "/app/data"
      volumes:
        - name: mealie-data
          emptyDir: {}
