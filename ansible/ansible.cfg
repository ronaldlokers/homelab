[defaults]
inventory = inventory.yml
host_key_checking = True
# Read-only by default is not a thing in Ansible, so the habit is: run with
# --check --diff first. Every task here is idempotent and safe to re-run.
[privilege_escalation]
become = True
become_method = sudo
