# Host state for the k3s control plane nodes (#159).
# Run with --check --diff first; see README.md.
---
- name: k3s control plane node configuration
  hosts: k3s_servers
  gather_facts: true
  # All three are control plane members; the journald restart is brief but real.
  serial: 1
  roles:
    - k3s_node
